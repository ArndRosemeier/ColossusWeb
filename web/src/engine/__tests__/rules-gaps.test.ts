/**
 * Remaining N/A digital dice etiquette only — other gaps closed by port.
 */
import { describe, it } from 'vitest'

describe('rules gaps (n/a)', () => {
  it.todo('L1: cocked / off-table dice are rerolled (N/A digital)')
  it.todo('L2: too many dice → reroll; hits capped (N/A digital)')
})
